| **I-POSE** | a body is `⟨original, pose, joints⟩` — original and pose **stored**, local field **derived**, and a body is **never stamped into the world field**. A robot's limb and a derailed wagon are the same case: an exact original at an arbitrary continuous pose. ⚠ **The AUTHORED body is a RIG and never contains a pose** (user, 2026-07-24): *"treat bodies we compose like meshes, so we define the bones and the limitations in the joints between the bones, never the actual pose."* So `original` = **⟨bones, joint limits⟩**, a static description in the shape of a stencil's `Form` — and `pose` is what a *running world* holds, never what an author writes. ⚠ **FLEX IS JOINTS, NOT DEFORMATION** (user, same day, on the aeroplane case): *"in any realistic scenario the wings have to be different defined objects — they are attached but not rigid, otherwise they would fail immediately. They can bend quite a bit upwards."* A part that bends is **more bones**, not a soft bone: the flex is expressed as joint values along a chain, and the aggregate bend is their composition. Consequence, and it is a large one — **hexbody never needs vertex deformation, skinning weights or a soft body**, and `I6` survives a flexing wing unchanged, because a bent wing is still a pure function of its joint values. `fits?` also stays local: each joint carries its own limit, and the wing's total travel is  Whether a saved world| Whether a saved world persists a pose or re-derives it is **open** ([`ASSESSMENT.md`](ASSESSMENT.md) §5) | §1.2 | stamp a free-posed body into the world lattice → it can only land on one of the 12 · **author a pose into a body definition → it is refused at the doorstep** |
